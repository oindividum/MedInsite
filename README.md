```markdown
MedInsite — Medical Image Analysis Platform

Микросервисная ML-платформа для анализа медицинских изображений**

📋 Оглавление
- 🌟 Особенности
- 🏗️ Архитектура  
- 🚀 Быстрый старт
- 🔧 Технологии
- 📁 Структура проекта
- ⚙️ Конфигурация
- 📊 Рабочий процесс
- 👨‍💻 Разработка
- 📈 Производительность
- ✨ Автор


```
🌟 Особенности

| Функциональность       | Описание                                         |
|------------------------|--------------------------------------------------|
| 🧠 **ML-инференс**     | Нейросетевой анализ медицинских изображений      |
| 📱 **Адаптивный UI**   | Мобильная версия и модальные окна результатов    |
| 🐳 **Контейнеризация** | Полная изоляция сервисов через Docker            |
| ⚡ **Микросервисы**    | Независимые сервисы с чёткой зоной ответственности |
| 🔌 **Асинхронность**   | Kafka для межсервисного взаимодействия           |
| 💾 **Хранилище**       | MinIO для медиафайлов и результатов              |


Детали сервисов

| Сервис                   | Назначение                                           | Технологии                         |
|--------------------------|-------------------------------------------------------|------------------------------------|
| **ApiService**           | Внешний API, фронтенд, маршрутизация, приём файлов   | FastAPI, HTML/CSS/JS, Jinja2       |
| **PreProcessorService**  | Подготовка данных: ресайз, нормализация, фильтры     | OpenCV, NumPy, PIL                 |
| **MLProcessorService**   | Основной inference, загрузка модели, обработка       | PyTorch/TensorFlow, OpenCV         |
| **MicroServiceToSaveMedia** | Сохранение медиа, выдача ссылок, работа с MinIO   | MinIO SDK, FastAPI                 |

```


🚀 Быстрый старт

Предварительные требования
- [Docker](https://docs.docker.com/get-docker/) 20.10+
- [Docker Compose](https://docs.docker.com/compose/install/) 2.0+
- 8 ГБ оперативной памяти (рекомендуется)
- 10 ГБ свободного места на диске

Установка и запуск

```bash
# 1. Клонирование репозитория
git clone https://github.com/oindividum/MedInsite.git
cd MedInsite

# 2. Запуск всех сервисов (первый запуск может занять 10-15 минут)
docker-compose up -d --build

# 3. Проверка статуса сервисов
docker-compose ps

# 4. Открытие приложения в браузере
# Frontend: http://localhost:8000
# API Docs: http://localhost:8000/docs
```

Полезные команды
```bash
# Просмотр логов всех сервисов
docker-compose logs -f

# Просмотр логов конкретного сервиса
docker-compose logs -f api-service

# Остановка всех сервисов
docker-compose down

# Полная пересборка (при изменениях в коде)
docker-compose up -d --build --force-recreate

# Очистка системы Docker
docker system prune -a --volumes
```

🔧 Технологии

| Слой                   | Технологии                                      |
|------------------------|--------------------------------------------------|
| **Backend**            | Python 3.10+, FastAPI, Pydantic, Uvicorn        |
| **ML/Data Processing** | OpenCV, NumPy, Pandas, PyTorch/TensorFlow       |
| **Message Queue**      | Apache Kafka, aiokafka, Redis                   |
| **Storage**            | MinIO, Local Filesystem                         |
| **Containerization**   | Docker, Docker Compose                          |
| **Frontend**           | HTML5, CSS3, JavaScript, Responsive Design      |
| **Monitoring**         | (планируется) Prometheus, Grafana               |

📁 Структура проекта

```
MedInsite/
├── ApiService/
│   ├── routes/           # Эндпоинты FastAPI
│   ├── services/         # Бизнес-логика
│   ├── static/           # Статические файлы (CSS, JS, images)
│   │   ├── css/
│   │   ├── js/
│   │   └── images/       # Аватарки, карточки результатов
│   ├── templates/        # HTML-шаблоны
│   ├── main.py          # Точка входа
│   └── requirements.txt
│
├── PreProcessorService/
│   ├── preprocessing/    # Логика предобработки
│   ├── consumers/        # Kafka consumers
│   └── requirements.txt
│
├── MLProcessorService/
│   ├── kafka/           # Обработка сообщений Kafka
│   ├── minio/           # Работа с хранилищем
│   ├── processor/       # Inference логика
│   ├── models/          # ML модели
│   └── requirements.txt
│
├── MicroServiceToSaveMedia/
│   ├── storage/         # Логика хранения
│   └── requirements.txt
│
├── docker-compose.yml    # Конфигурация Docker Compose
├── .env.example          # Пример переменных окружения
├── .gitignore           # Исключения Git
├── README.md            # Этот файл
└── Architecture1.png    # Схема архитектуры
```

⚙️ Конфигурация

Переменные окружения
Создайте файл `.env` в корне проекта на основе `.env.example`:

```env
# Общие настройки
DEBUG=false
LOG_LEVEL=INFO

# Kafka
KAFKA_BOOTSTRAP_SERVERS=kafka:9092
KAFKA_TOPIC_INPUT=image-processing-input
KAFKA_TOPIC_OUTPUT=image-processing-output

# MinIO
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_NAME=medical-images

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# Порты сервисов
API_SERVICE_PORT=8000
PREPROCESSOR_PORT=8001
ML_PROCESSOR_PORT=8002
STORAGE_SERVICE_PORT=8003
```

Настройка ML-модели
1. Поместите файлы модели в `MLProcessorService/models/`
2. Обновите конфигурацию в `MLProcessorService/processor/config.py`
3. При необходимости измените параметры inference

📊 Рабочий процесс

Полный цикл обработки
1. **Загрузка изображения**: Пользователь загружает медицинское изображение через веб-интерфейс
2. **API-обработка**: ApiService валидирует файл и отправляет задачу в Kafka
3. **Предобработка**: PreProcessorService получает задачу, выполняет:
   - Ресайз изображения
   - Нормализацию пикселей
   - Применение фильтров
4. **ML-инференс**: MLProcessorService:
   - Загружает подготовленное изображение
   - Выполняет предсказание с помощью нейросети
   - Формирует результат
5. **Сохранение**: MicroServiceToSaveMedia:
   - Сохраняет оригинальное изображение и результат
   - Генерирует уникальные ссылки
6. **Возврат результата**: ApiService получает результат и отображает его пользователю

Пример запроса API
```bash
curl -X POST "http://localhost:8000/api/v1/analyze" \
  -H "Content-Type: multipart/form-data" \
  -F "image=@path/to/medical_image.png" \
  -F "patient_id=12345"
```

👨‍💻 Разработка

Локальная разработка (без Docker)
```bash
# 1. Установка зависимостей для каждого сервиса
cd ApiService
python -m venv venv
source venv/bin/activate  # или venv\Scripts\activate на Windows
pip install -r requirements.txt

# 2. Запуск сервисов в отдельных терминалах
# Сервис 1
cd ApiService
uvicorn main:app --reload --port 8000

# Сервис 2
cd PreProcessorService
python main.py

# и т.д.
```

Тестирование
```bash
# Запуск тестов для конкретного сервиса
cd ApiService
pytest tests/

# Запуск всех тестов
python -m pytest discover -s
```

📈 Производительность

Рекомендации по оптимизации
1. **Batch-обработка**: Передавайте изображения пачками для ускорения inference
2. **Кэширование**: Используйте Redis для кэширования промежуточных результатов
3. **Масштабирование**: Каждый сервис можно масштабировать независимо
4. **Мониторинг**: Добавьте метрики Prometheus для отслеживания производительности

Известные ограничения
- Первый запуск занимает время (скачивание зависимостей и ML-моделей)
- Обработка больших изображений может быть медленной
- Требуется достаточный объем оперативной памяти для ML-моделей

✨ Автор

**Ойдин**  
Москва, 2025
```
